<div class="hotel-item">
    <h2>{{ item.name }}</h2>

    <div class="hotel-item-row">

        <div class="row">

            <div class="col-md-8">
                Rating: {{ item.rating }}<br>
                Preis: {{ item.price|number_format(2, ',') }} €<br>
                {{ item.description }}
            </div>

            <div class="col-md-4">
                <img style="max-width: 100%" src="{{ item.imageURL }}">
            </div>

        </div>

    </div>

</div>